{
    'use strict';

    // config返却
    function getconfig() {
        return {
            INDEX_DB : {
                DB_NAME : 'ToriiTestDB',
                DB_VERSION : 1,
            }
        };
    }
}
